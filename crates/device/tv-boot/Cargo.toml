[package]
name = "tv-boot"
version = "0.0.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "Secure boot chain, TPM measurement, and key unsealing."
repository = "https://github.com/acenji/trustvoting"
publish = false

[dependencies]
tv-formats = { path = "../../shared/tv-formats" }
