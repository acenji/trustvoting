[package]
name = "tv-export"
version = "0.0.0"
edition = "2021"
license = "AGPL-3.0-only"
description = "Signed export-bundle creation and sealed-media write."
repository = "https://github.com/acenji/trustvoting"
publish = false

[dependencies]
tv-formats = { path = "../../shared/tv-formats" }
